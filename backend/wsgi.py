# =====================================================
# wsgi.py - نقطة الدخول لتشغيل التطبيق مع Gunicorn
# الإصدار 2.0 - للاستخدام في بيئة الإنتاج
# =====================================================

from app import create_app

# إنشاء تطبيق Flask
app = create_app()

# هذا الملف يُستخدم بواسطة Gunicorn لبدء تشغيل الخادم
# الأمر: gunicorn --bind 0.0.0.0:5000 wsgi:app

if __name__ == "__main__":
    # للتشغيل المباشر (للاختبار فقط)
    app.run(host='0.0.0.0', port=5000, debug=True)